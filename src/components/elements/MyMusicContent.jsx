import React from 'react';
// import myTrack from '../../API/TrackListAdd.js'
import TracItem from './TracItem';

function MyMusicContent() {
	return (
		<div>
			<div className=""></div>
			<div className="">
				{
					// myTrack.map((item, index) =>
					// 	< TracItem key={"MyTrack" + item.trackName + item.trackExecutor} number={index + 1} trackName={item.trackName} trackExecutor={item.trackExecutor} data-tracklink={item.trackLink} likeIcon="true" />
					// )
				}
			</div>

		</div>
	)
}

export default MyMusicContent